# Dependency versions are declared once, in pyproject.toml.
# This file exists only so `pip install -r requirements.txt` keeps working
# for readers who skip the "editable install" step in the README.
-e .[app,api,dev]
